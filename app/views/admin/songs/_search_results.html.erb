<% @songs.each do |song| %>
    <%= render :partial => 'song', :locals=>{
      :song=>song, :album=>song.album, :playlists=>song.playlists
      } %>
<% end %>
<script type="text/javascript" charset="utf-8">
  var AUTH_TOKEN = "<%= form_authenticity_token %>";
  bind_song_functions();
</script>